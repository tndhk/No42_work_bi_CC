FROM python:3.11-slim

WORKDIR /app

# 依存ライブラリインストール
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# アプリケーションコピー (ボリュームマウントで上書きされる)
COPY . .

# 開発サーバー起動
CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]
